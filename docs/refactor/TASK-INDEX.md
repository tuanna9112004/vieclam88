# Task Index — Refactor Playbook (deprecated, chỉ còn lịch sử)

> **Deprecated cho task mới.** Từ TASK 0.2, nguồn KEY/GATE/DONE/NEXT chính thức của mọi
> `TASK x.y` là `docs/VIECLAM88_TASK_REGISTRY_V2.3.md` (đọc qua `/task-cycle`). File này và
> `docs/refactor/tasks/` chỉ còn giữ lại làm hồ sơ lịch sử cho TASK 0.1 — không thêm dòng mới,
> không dùng để tra task tiếp theo.

| Mã task | Tên task | Command chính | Dependency | File task |
|---|---|---|---|---|
| TASK 0.1 | Lập baseline kỹ thuật trước khi sửa | `/implement` | Không (task đầu tiên) | [`tasks/TASK-0.1.md`](tasks/TASK-0.1.md) |

## Quy tắc dùng bảng này (lịch sử)

- Bảng trên không mở rộng thêm; không suy diễn nội dung TASK 0.2 trở đi từ đây.
- Không bắt đầu một `TASK x.y` khi dependency của nó chưa `DONE` theo `docs/PROJECT-STATUS.md`.
